{
  "mcpServers": {
    "veriforge": {
      "command": "python",
      "args": ["-m", "veriforge_mcp.server", "--transport", "stdio"],
      "description": "VeriForge MCP Server — 8 security tools: code verification, spec generation, compliance checks, audit chain, spec refinement, test generation, security scanning, finding explanation",
      "env": {
        "PYTHONPATH": "/path/to/veriforge_mcp"
      }
    }
  }
}
